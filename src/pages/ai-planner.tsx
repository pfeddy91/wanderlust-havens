import React from 'react';
import AiPlannerContainer from '@/components/ai-planner/AiPlannerContainer'; // Adjust the import path if necessary
// import Layout from '@/components/Layout'; // Optional: If you have a main layout component

const PlannerPage = () => {
  return (
    // Optional: Wrap with your site layout
    // <Layout>
      <AiPlannerContainer />
    // </Layout>
  );
};

export default PlannerPage; 